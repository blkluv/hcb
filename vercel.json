{
  "builds": [
    {
      "src": "Gemfile",
      "use": "@vercel/ruby",
      "config": {
        "installCommand": "apt-get update && apt-get install -y libyaml-dev && bundle install"
      }
    }
  ]
}
